import 'package:flutter/material.dart';

class ReciepsView extends StatelessWidget {
  const ReciepsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
